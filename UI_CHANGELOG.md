## V3 executive readability

Requested after visual review of the deployed dashboard.

1. KPI labels expanded so every card explains what the number means.
2. Executive KPI values rounded to whole numbers where appropriate.
3. Metric CSS prevents truncated labels/values.
4. Exporter ranking chart hides Plotly toolbar to prevent overlap.
5. Product analytical chart keeps only useful controls and disables scroll zoom.
6. Visible pilot labels removed.
7. Underlying analysis functions are unchanged.
